#!/usr/bin/env python
import os

def main():

	just_a_list = ['first', 'second', 'third']

	for index, item in enumerate(just_a_list):
		print(str(index) + str(item))

if __name__ == '__main__':
	main()
